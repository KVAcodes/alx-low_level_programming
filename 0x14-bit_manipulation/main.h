#ifndef MAIN_H
#define MAIN_H
#include <stdio.h>
#include <stdlib.h>
unsigned int binary_to_uint(const char *b);
int power(int x, int y);
void print_binary(unsigned long int n);
int _putchar(char c);

#endif /* MAIN_H */
